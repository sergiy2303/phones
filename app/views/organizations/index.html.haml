.row
  .col-md-8
    .navbar.navbar-default{:role=>"navigation"}>
      .container-fluid
        .navbar-header
          .navbar-brand 
            Create you list you company stucrure by us
        .collapse.navbar-collapse
        #bs-example-navbar-collapse-1
        %ul.nav.navbar-nav
          %li= link_to 'FIRST LINES', lines_path
          %li= link_to 'ADD COMPANY', new_organization_path(), :type=> "button", :class => "btn btn-default"
  .col-md-4
    .page-header
      %h3 Try create contact book with rails
      %small using rails and bootstrap
.row
  .col-md-4
    .panel.panel-success
      .panel-heading
        %h4 Here is all companies
      %ul.list-group
      - @organizations.each do |organization|
        %li.list-group-item
          = link_to 'Remove', organization_path(organization), method: :delete, :class => 'btn btn-danger'
          = link_to 'edit', edit_organization_path(organization), :class => 'btn btn-warning'
          = link_to organization.name, organization_path(organization), :class => 'btn btn-success'
  .col-md-8
