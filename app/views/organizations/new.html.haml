.panel.panel-primary
  .panel-heading
    NEW
  .panel-body
    = bootstrap_form_for :organization, url: organizations_path do |f|
      = f.text_field :name
      = f.text_field :address
      = f.text_field :phone
      = f.text_field :email
      = f.submit :class => "btn btn-primary"
      = link_to 'CANCEL', organizations_path(), :type=> "button", :class => "btn btn-danger"
