= form_for :personal, url: organization_departament_personals_path(organization_id: @organization.id, departametn_id: @departament.id) do |f|
  = f.label :full_name
  = f.text_field :full_name
  = f.submit

= @organization.id
%br
= @departament.id